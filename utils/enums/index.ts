export enum UserRole {
  Admin = 'Admin',
  User = 'User',
}

export enum StatusEnum {
  Active = 'Active',
  Inactive = 'Inactive',
}

export enum StreamStatusEnum {
  Live = 'Live',
  Offline = 'Offline',
  Upcoming = 'Upcoming',
  Past = 'Past',
}
