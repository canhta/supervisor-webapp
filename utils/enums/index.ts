export enum UserRole {
  Admin = 'Admin',
  User = 'User',
}

export enum StatusEnum {
  Active = 'Active',
  Inactive = 'Inactive',
}

export enum StreamStatusEnum {
  Upcoming = 'Upcoming',
  Starting = 'Starting',
  Live = 'Live',
  Stopping = 'Stopping',
  Offline = 'Offline',
}
